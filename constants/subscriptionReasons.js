/**
 * Machine-readable reasons for plan-generation eligibility (API + logs).
 * All values are stable English enums for clients and tests.
 */
export const PLAN_GEN_REASON = {
  SUBSCRIBED: "subscribed",
  FREE_PLAN: "free_plan",
  SUBSCRIPTION_REQUIRED: "subscription_required",
};
